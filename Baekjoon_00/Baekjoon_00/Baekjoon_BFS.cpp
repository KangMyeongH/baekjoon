#include "Baekjoon_BFS.h"

#include <iostream>
#include <vector>

int Baekjoon_BFS::Solution()
{
	std::cin.tie(nullptr);
	std::ios::sync_with_stdio(false);

	int n, m;
	std::cin >> n >> m;

	std::vector<std::vector<int>> 

	for (int i = 0; i < n; ++i)
	{
		for (int j = 0; j < m; ++j)
		{

		}
	}

	return 0;
}
